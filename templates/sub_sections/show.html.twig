{% extends 'base.html.twig' %}

{% block title %}SubSections{% endblock %}

{% block body %}
    <h1>SubSections</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_section.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_section.name }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ sub_section.link }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ sub_section.position }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sub_sections_index') }}">back to list</a>

    <a href="{{ path('sub_sections_edit', {'id': sub_section.id}) }}">edit</a>

    {{ include('sub_sections/_delete_form.html.twig') }}
{% endblock %}
