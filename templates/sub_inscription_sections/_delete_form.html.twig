<form method="post" action="{{ path('sub_inscription_sections_delete', {'id': sub_inscription_section.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_inscription_section.id) }}">
    <button class="btn">Delete</button>
</form>
